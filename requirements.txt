aiofiles
bs4
certifi
dnspython
# pyrogram
# kurigram>=2.2.18
https://github.com/KurimuzonAkuma/kurigram/archive/dev.zip

httpx
dotenv
markdown
motor
gtts
psutil
hachoir
# googletrans==4.0.0-rc1
cachetools
tinytag
pyromod
python-dotenv
# pyyaml
PyYAML==6.0.3
aiohttp
requests
telegraph
deep-translator
httpx>=0.27.0
tgcrypto; sys_platform != 'win32'
pycryptodome
ujson
natsort
gitpython
pillow
ffmpeg-python
numpy

# Performance optimization (Linux/macOS only, auto-skip on Windows)
uvloop>=0.19.0; sys_platform != 'win32'

# Performance optimization (Windows only)
winloop>=0.1.0; sys_platform == 'win32'

# Dependencies for the Cache System (select according to your needs)

# For the Redis backend (recommended for production)
redis>=4.5.0

# For MongoDB backend
pymongo>=4.3.0
motor>=3.1.0

# for xforwrad_pro.py
Pillow
googletrans-py
moviepy
aiosqlite
# pyrogram
tgcrypto; sys_platform != 'win32'
pycryptodome
python-magic; sys_platform != 'win32'
python-magic-bin; sys_platform == 'win32'

# Dependencies that may already exist in AltruixX:
# pyrogram>=2.0.0
# aiofiles>=0.8.0
# python-dotenv>=0.19.0

# - For Redis: pip install redis
# - For MongoDB: pip install pymongo motor
# - No external database: no need to install anything (use default JSON)

# ✅ Ultroid Addons Compatibility
telethon
# wikipedia
# htmlwebshot
# enhancer
# catbox-uploader
# cloudscraper
# cv2
# opencv-python
# twikit 
# speedtest
# shazamio
# pytz
# cryptg
# jikanpy
# google_trans_new
# covid
# pyfiglet
# pyjokes
# pygments
# PyPDF2
# pokedex
# qrcode
# emoji
# quotefancy
# lyrics_extractor
# speech_recognition
# textblob

# phlogo
# fontTools
# ssl

colorama
